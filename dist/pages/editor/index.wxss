/*!**************************************************************************************************************************************************************************************************!*\
  !*** css ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[4].oneOf[0].use[1]!./node_modules/postcss-loader/dist/cjs.js??ruleSet[1].rules[4].oneOf[0].use[2]!./src/pages/editor/index.css ***!
  \**************************************************************************************************************************************************************************************************/
.editor {
  min-height: 100vh;
  background-color: #FAFAF9;
  position: relative;
}

.editor-content {
  padding: 20rpx;
  height: 100vh;
  display: flex;
  flex-direction: column;
}

/* 客服按钮 */

.customer-service-btn {
  position: fixed;
  bottom: 100rpx;
  right: 20rpx;
  width: 60rpx;
  height: 60rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #7E22CE 0%, #9333EA 100%);
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  box-shadow: 0 4rpx 12rpx rgba(126, 34, 206, 0.3);
  z-index: 100;
  cursor: pointer;
  transition: all 0.3s ease;
  backdrop-filter: blur(10rpx);
  border: 1rpx solid rgba(147, 51, 234, 0.3);
}

.customer-service-btn:hover {
  background: linear-gradient(135deg, #9333EA 0%, #A855F7 100%);
  transform: scale(1.05);
  box-shadow: 0 8rpx 24rpx rgba(126, 34, 206, 0.4);
}

.customer-service-btn:active {
  background: linear-gradient(135deg, #6B21A8 0%, #7E22CE 100%);
  transform: scale(0.95);
}

.service-icon {
  font-size: 24rpx;
  margin-bottom: 2rpx;
}

.service-text {
  font-size: 12rpx;
  color: #fff;
  font-weight: 700;
}

/* 顶部固定高度的图片预览区域 */

.image-preview-section {
  background: #fff;
  border-radius: 20rpx;
  padding: 24rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 8rpx 24rpx rgba(126, 34, 206, 0.1);
  min-height: 300rpx;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  border: 1rpx solid rgba(126, 34, 206, 0.2);
  backdrop-filter: blur(10rpx);
}

/* 上传占位符 */

.upload-placeholder {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  cursor: pointer;
  background: linear-gradient(135deg, #FAFAF9 0%, #F5F5F4 100%);
  border: 3rpx dashed #9333EA;
  border-radius: 20rpx;
  padding: 60rpx 30rpx;
  text-align: center;
  margin-bottom: 15rpx;
  transition: all 0.3s ease;
  backdrop-filter: blur(5rpx);
}

.upload-placeholder:hover {
  background: linear-gradient(135deg, #F5F5F4 0%, #E7E5E4 100%);
  border-color: #A855F7;
  transform: scale(1.02);
  box-shadow: 0 12rpx 24rpx rgba(126, 34, 206, 0.2);
}

.upload-icon {
  font-size: 72rpx;
  margin-bottom: 20rpx;
  color: #9333EA;
}

.upload-text {
  font-size: 20rpx;
  color: #1C1917;
  text-align: center;
  font-weight: 700;
}

/* 图片预览 */

.preview-image {
  width: 100%;
  flex: 1;
  min-height: 200rpx;
  border-radius: 16rpx;
  background: #FAFAF9;
  margin-bottom: 20rpx;
  border: 1rpx solid rgba(126, 34, 206, 0.2);
}

/* 图片信息栏 */

.image-info-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 20rpx;
  border-top: 1rpx solid rgba(126, 34, 206, 0.2);
  font-size: 16rpx;
  color: #1C1917;
  font-weight: 600;
}

.image-size-text {
  flex: 1;
  text-align: center;
  font-weight: 700;
}

.info-separator {
  margin: 0 12rpx;
  color: #9333EA;
  font-size: 20rpx;
}

/* 中间可滚动操作区域 */

.scrollable-content {
  flex: 1;
  overflow-y: auto;
  margin-bottom: 15rpx;
  padding: 0 15rpx;
}

/* 上传占位符 */

.upload-placeholder {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  cursor: pointer;
  background: #f9f9f9;
  border: 2rpx dashed #ddd;
  border-radius: 8rpx;
  padding: 60rpx 30rpx;
  text-align: center;
  margin-bottom: 15rpx;
}

/* 功能操作区域 */

.options-section {
  background: #fff;
  border-radius: 20rpx;
  padding: 24rpx;
  box-shadow: 0 8rpx 24rpx rgba(126, 34, 206, 0.1);
  margin-bottom: 24rpx;
  border: 1rpx solid rgba(126, 34, 206, 0.2);
  backdrop-filter: blur(10rpx);
}

.options-title {
  font-size: 22rpx;
  font-weight: 700;
  color: #1C1917;
  margin-bottom: 24rpx;
  display: block;
}

/* 底部固定按钮区域 */

.bottom-buttons {
  display: flex;
  gap: 16rpx;
  margin-top: auto;
  padding: 24rpx 15rpx;
  background: #fff;
  border-top: 1rpx solid rgba(126, 34, 206, 0.2);
  backdrop-filter: blur(10rpx);
}

.bottom-btn {
  flex: 1;
  padding: 18rpx 24rpx;
  border: none;
  border-radius: 16rpx;
  font-size: 18rpx;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.3s ease;
}

.bottom-btn:hover {
  transform: scale(1.05);
  box-shadow: 0 8rpx 24rpx rgba(126, 34, 206, 0.3);
}

.bottom-btn:active {
  transform: scale(0.95);
}

.select-btn {
  background: #fff;
  color: #9333EA;
  border: 2rpx solid #9333EA;
}

.select-btn:hover {
  background: rgba(147, 51, 234, 0.05);
  border-color: #A855F7;
}

.save-btn {
  background: linear-gradient(135deg, #EA580C 0%, #FB923C 100%);
  color: #fff;
  box-shadow: 0 4rpx 12rpx rgba(234, 88, 12, 0.3);
}

.save-btn:hover {
  background: linear-gradient(135deg, #FB923C 0%, #FDBA74 100%);
  box-shadow: 0 8rpx 24rpx rgba(234, 88, 12, 0.4);
}

.save-btn:disabled {
  background: #E7E5E4;
  cursor: not-allowed;
  box-shadow: none;
}

/* 加载状态 */

.loading-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
}

.loading-text {
  color: #fff;
  font-size: 18rpx;
  font-weight: bold;
}

/* 操作按钮 */

.action-btn {
  width: 100%;
  background: linear-gradient(135deg, #7E22CE 0%, #9333EA 100%);
  color: #fff;
  border: none;
  border-radius: 16rpx;
  padding: 16rpx 24rpx;
  font-size: 18rpx;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.3s ease;
  margin-bottom: 16rpx;
  box-shadow: 0 4rpx 12rpx rgba(126, 34, 206, 0.2);
  backdrop-filter: blur(5rpx);
}

.action-btn:hover {
  background: linear-gradient(135deg, #9333EA 0%, #A855F7 100%);
  transform: scale(1.05);
  box-shadow: 0 8rpx 24rpx rgba(126, 34, 206, 0.3);
}

.action-btn:active {
  background: linear-gradient(135deg, #6B21A8 0%, #7E22CE 100%);
  transform: scale(0.95);
}

/* 格式转换网格布局 */

.format-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12rpx;
  margin-bottom: 20rpx;
}

.format-btn {
  background: #fff;
  color: #020617;
  border: 2rpx solid #E2E8F0;
  border-radius: 12rpx;
  padding: 14rpx 15rpx;
  font-size: 14rpx;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s ease;
}

.format-btn:hover {
  background: #F1F5F9;
  border-color: #0369A1;
}

.format-btn:active {
  background: #DBEAFE;
  border-color: #0284c7;
}

/* 编辑按钮组 */

.edit-buttons {
  display: flex;
  gap: 10rpx;
  margin-bottom: 15rpx;
}

.edit-buttons .action-btn {
  flex: 1;
  margin-bottom: 0;
}

/* 滤镜按钮组 */

.filter-buttons {
  display: flex;
  gap: 10rpx;
  margin-bottom: 15rpx;
}

.filter-buttons .action-btn {
  flex: 1;
  margin-bottom: 0;
}

/* 压缩质量滑块 */

.slider-container {
  margin: 20rpx 0;
}

.slider {
  width: 100%;
  height: 8rpx;
  border-radius: 4rpx;
  background: #E2E8F0;
  outline: none;
  -webkit-appearance: none;
}

.slider::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: 24rpx;
  height: 24rpx;
  border-radius: 50%;
  background: #0369A1;
  cursor: pointer;
  -webkit-transition: transform 0.2s ease;
  transition: transform 0.2s ease;
}

.slider::-webkit-slider-thumb:hover {
  transform: scale(1.1);
  background: #0284c7;
}

.slider::-moz-range-thumb {
  width: 24rpx;
  height: 24rpx;
  border-radius: 50%;
  background: #0369A1;
  cursor: pointer;
  border: none;
  -moz-transition: transform 0.2s ease;
  transition: transform 0.2s ease;
}

.slider::-moz-range-thumb:hover {
  transform: scale(1.1);
  background: #0284c7;
}

/* 输入框样式 */

.input-group {
  margin-bottom: 20rpx;
}

.input-label {
  font-size: 15rpx;
  color: #020617;
  margin-bottom: 8rpx;
  display: block;
  font-weight: 500;
}

.input-field {
  width: 100%;
  padding: 12rpx 16rpx;
  border: 2rpx solid #E2E8F0;
  border-radius: 12rpx;
  font-size: 16rpx;
  transition: all 0.2s ease;
  background: #fff;
}

.input-field:focus {
  border-color: #0369A1;
  outline: none;
}

/* 复选框样式 */

.checkbox-group {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 20rpx;
}

.checkbox-label {
  font-size: 15rpx;
  color: #020617;
  font-weight: 500;
}

/* 尺寸选项列表 */

.size-option {
  margin-bottom: 20rpx;
}

.size-option-label {
  font-size: 15rpx;
  color: #020617;
  margin-bottom: 10rpx;
  display: block;
  font-weight: 500;
}

.size-option-btn {
  width: 100%;
  background: #0369A1;
  color: #fff;
  border: none;
  border-radius: 12rpx;
  padding: 14rpx 20rpx;
  font-size: 15rpx;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s ease;
}

.size-option-btn:hover {
  background: #0284c7;
}

.size-option-btn:active {
  background: #014e7a;
}

/* 格式选择区域 */

.format-selector {
  margin-bottom: 15rpx;
}

.format-selector-title {
  font-size: 14rpx;
  color: #333;
  margin-bottom: 10rpx;
  display: block;
  font-weight: 500;
}

.format-options {
  display: flex;
  gap: 15rpx;
  align-items: center;
}

.format-option {
  display: flex;
  align-items: center;
  gap: 5rpx;
}

.format-checkbox {
  width: 18rpx;
  height: 18rpx;
  border: 2rpx solid #ddd;
  border-radius: 4rpx;
  display: flex;
  justify-content: center;
  align-items: center;
}

.format-checkbox.checked {
  background: #667eea;
  border-color: #667eea;
}

.format-checkbox-icon {
  font-size: 12rpx;
  color: #fff;
}

.format-checkbox-label {
  font-size: 14rpx;
  color: #666;
}
