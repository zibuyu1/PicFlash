/*!**************************************************************************************************************************************************************************************************!*\
  !*** css ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[4].oneOf[0].use[1]!./node_modules/postcss-loader/dist/cjs.js??ruleSet[1].rules[4].oneOf[0].use[2]!./src/pages/editor/index.css ***!
  \**************************************************************************************************************************************************************************************************/
.editor {
  min-height: 100vh;
  background-color: var(--background);
  position: relative;
  overflow: hidden;
}

.editor::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: radial-gradient(circle at 30% 40%, rgba(99, 102, 241, 0.1) 0%, transparent 50%),
              radial-gradient(circle at 70% 60%, rgba(236, 72, 153, 0.1) 0%, transparent 50%);
  pointer-events: none;
}

.editor-content {
  padding: 20rpx;
  height: 100vh;
  display: flex;
  flex-direction: column;
  position: relative;
  z-index: 1;
  gap: 20rpx;
}

/* 客服按钮 */

.customer-service-btn {
  position: fixed;
  bottom: 120rpx;
  right: 20rpx;
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  box-shadow: var(--neon-glow), 0 4rpx 12rpx rgba(99, 102, 241, 0.4);
  z-index: 100;
  cursor: pointer;
  transition: all 0.3s ease;
  border: 1rpx solid rgba(99, 102, 241, 0.4);
}

.customer-service-btn:hover {
  background: linear-gradient(135deg, var(--primary-light) 0%, rgba(129, 140, 248, 0.8) 100%);
  transform: scale(1.05);
  box-shadow: var(--neon-glow), 0 8rpx 24rpx rgba(99, 102, 241, 0.6);
}

.customer-service-btn:active {
  background: linear-gradient(135deg, var(--primary) 0%, rgba(99, 102, 241, 0.8) 100%);
  transform: scale(0.95);
}

.service-icon {
  font-size: 24rpx;
  margin-bottom: 2rpx;
  filter: drop-shadow(0 0 5rpx rgba(255, 255, 255, 0.5));
}

.service-text {
  font-size: 12rpx;
  color: var(--text);
  font-weight: 700;
  text-shadow: 0 0 5rpx rgba(99, 102, 241, 0.5);
}

/* 顶部固定高度的图片预览区�?*/

.image-preview-section {
  height: 100%;
  position: relative;
  overflow: hidden;
}

/* 上传占位�?*/

.upload-content{
  height: 400rpx;
  background: rgba(26, 26, 46, 0.6);
  border: 3rpx dashed rgba(99, 102, 241, 0.4);
  border-radius: 20rpx;
}

.upload-placeholder {
  height: 100%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  position: relative;
  overflow: hidden;
}

.upload-icon {
  font-size: 72rpx;
  margin-bottom: 20rpx;
  color: var(--primary);
  filter: drop-shadow(0 0 10rpx var(--primary));
}

.upload-text {
  font-size: 20rpx;
  color: var(--text);
  text-align: center;
  font-weight: 700;
  text-shadow: 0 0 5rpx rgba(255, 255, 255, 0.1);
}

/* 图片预览 */

.preview-image {
  width: 100%;
  height: 100%;
}

/* 图片信息�?*/

.image-info-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 20rpx;
  border-top: 1rpx solid var(--border);
  font-size: 14rpx;
  color: var(--text-secondary);
  font-weight: 600;
  position: relative;
  z-index: 1;
}

.image-size-text {
  flex: 1;
  text-align: center;
  font-weight: 700;
  text-shadow: 0 0 5rpx rgba(99, 102, 241, 0.3);
}

.info-separator {
  margin: 0 12rpx;
  color: var(--primary);
  font-size: 20rpx;
  text-shadow: 0 0 5rpx var(--primary);
}

/* 中间可滚动操作区�?*/

.scrollable-content {
  flex: 1;
  overflow-y: auto;
  position: relative;
  z-index: 1;
}

/* 功能操作区域 */

.options-section {
  background: rgba(26, 26, 46, 0.8);
  backdrop-filter: blur(12rpx);
  border: 1rpx solid rgba(99, 102, 241, 0.2);
  border-radius: 20rpx;
  padding: 24rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.3);
  margin-bottom: 24rpx;
  position: relative;
  overflow: hidden;
}

.options-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(99, 102, 241, 0.05), transparent);
  transition: left 0.5s ease;
}

.options-section:hover::before {
  left: 100%;
}

.options-title {
  font-family: 'Fredoka', sans-serif;
  font-size: 22rpx;
  font-weight: 700;
  color: var(--text);
  margin-bottom: 24rpx;
  display: block;
  text-shadow: 0 0 5rpx rgba(99, 102, 241, 0.3);
}

/* 底部固定按钮区域 */

.bottom-buttons {
  display: flex;
  gap: 16rpx;
  margin-top: auto;
  padding: 24rpx 15rpx;
  background: rgba(26, 26, 46, 0.9);
  backdrop-filter: blur(12rpx);
  border-top: 1rpx solid var(--border);
  position: relative;
  z-index: 1;
}

.bottom-btn {
  flex: 1;
  padding: 18rpx 0;
  border: none;
  border-radius: 16rpx;
  font-family: 'Fredoka', sans-serif;
  font-size: 26rpx;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.bottom-btn::before {
  content: '';
  position: absolute;
  top: -2rpx;
  left: -2rpx;
  right: -2rpx;
  bottom: -2rpx;
  background: linear-gradient(45deg, var(--primary), var(--secondary), var(--cta), var(--primary));
  border-radius: 16rpx;
  z-index: -1;
  opacity: 0;
  transition: opacity 0.3s ease;
  animation: gradientMove 3s linear infinite;
}

.bottom-btn:hover::before {
  opacity: 0.5;
}

.bottom-btn:hover {
  transform: translateY(-2rpx);
  box-shadow: var(--neon-glow);
}

.bottom-btn:active {
  transform: translateY(0);
}

.select-btn {
  background: transparent;
  color: var(--primary);
  border: 2rpx solid var(--primary);
  box-shadow: 0 0 10rpx rgba(99, 102, 241, 0.2);
}

.select-btn:hover {
  background: rgba(99, 102, 241, 0.1);
  border-color: var(--primary-light);
  box-shadow: var(--neon-glow);
}

.save-btn {
  background: linear-gradient(135deg, var(--cta), #10B981);
  color: var(--text);
  box-shadow: 0 4rpx 12rpx rgba(34, 197, 94, 0.3);
}

.save-btn:hover {
  background: linear-gradient(135deg, #10B981, var(--cta));
  box-shadow: var(--neon-glow), 0 8rpx 24rpx rgba(34, 197, 94, 0.5);
}

.save-btn:disabled {
  background: rgba(51, 65, 85, 0.5);
  cursor: not-allowed;
  box-shadow: none;
  border-color: var(--border);
}

/* 加载状�?*/

.loading-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(15, 15, 26, 0.8);
  backdrop-filter: blur(10rpx);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
  border: 1rpx solid var(--border);
}

.loading-text {
  color: var(--text);
  font-size: 18rpx;
  font-weight: bold;
  text-shadow: var(--neon-glow);
  animation: pulse 1.5s infinite ease-in-out;
}

/* 操作按钮 */

.action-btn {
  width: 100%;
  background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
  color: var(--text);
  border: none;
  border-radius: 12rpx;
  padding: 16rpx 24rpx;
  font-family: 'Fredoka', sans-serif;
  font-size: 16rpx;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  margin-bottom: 16rpx;
  box-shadow: 0 4rpx 12rpx rgba(99, 102, 241, 0.3);
  backdrop-filter: blur(8rpx);
  position: relative;
  overflow: hidden;
}

.action-btn::before {
  content: '';
  position: absolute;
  top: -2rpx;
  left: -2rpx;
  right: -2rpx;
  bottom: -2rpx;
  background: linear-gradient(45deg, var(--primary), var(--secondary), var(--primary-light), var(--primary));
  border-radius: 12rpx;
  z-index: -1;
  opacity: 0;
  transition: opacity 0.3s ease;
  animation: gradientMove 3s linear infinite;
}

.action-btn:hover::before {
  opacity: 0.5;
}

.action-btn:hover {
  background: linear-gradient(135deg, var(--primary-light) 0%, rgba(129, 140, 248, 0.8) 100%);
  transform: translateY(-2rpx);
  box-shadow: var(--neon-glow), 0 8rpx 24rpx rgba(99, 102, 241, 0.5);
}

.action-btn:active {
  background: linear-gradient(135deg, var(--primary) 0%, rgba(99, 102, 241, 0.8) 100%);
  transform: translateY(0);
}

/* 格式转换网格布局 */

.format-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12rpx;
  margin-bottom: 20rpx;
}

.format-btn {
  background: rgba(26, 26, 46, 0.8);
  color: var(--text);
  border: 2rpx solid var(--border);
  border-radius: 12rpx;
  padding: 14rpx 15rpx;
  font-family: 'Fredoka', sans-serif;
  font-size: 14rpx;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  backdrop-filter: blur(8rpx);
}

.format-btn:hover {
  background: rgba(99, 102, 241, 0.1);
  border-color: var(--primary);
  box-shadow: 0 0 10rpx rgba(99, 102, 241, 0.3);
}

.format-btn:active {
  background: rgba(99, 102, 241, 0.2);
  border-color: var(--primary-light);
}

/* 编辑按钮�?*/

.edit-buttons {
  display: flex;
  gap: 10rpx;
  margin-bottom: 15rpx;
}

.edit-buttons .action-btn {
  flex: 1;
  margin-bottom: 0;
}

/* 滤镜按钮�?*/

.filter-buttons {
  display: flex;
  gap: 10rpx;
  margin-bottom: 15rpx;
}

.filter-buttons .action-btn {
  flex: 1;
  margin-bottom: 0;
}

/* 压缩质量滑块 */

.slider-container {
  margin: 20rpx 0;
}

.slider {
  width: 100%;
  height: 8rpx;
  border-radius: 4rpx;
  background: rgba(51, 65, 85, 0.5);
  outline: none;
  -webkit-appearance: none;
  box-shadow: inset 0 2rpx 4rpx rgba(0, 0, 0, 0.3);
}

.slider::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: 24rpx;
  height: 24rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--primary), var(--primary-light));
  cursor: pointer;
  -webkit-transition: transform 0.2s ease;
  transition: transform 0.2s ease;
  box-shadow: 0 2rpx 8rpx rgba(99, 102, 241, 0.5);
}

.slider::-webkit-slider-thumb:hover {
  transform: scale(1.1);
  box-shadow: 0 4rpx 12rpx rgba(99, 102, 241, 0.7);
}

.slider::-moz-range-thumb {
  width: 24rpx;
  height: 24rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--primary), var(--primary-light));
  cursor: pointer;
  border: none;
  -moz-transition: transform 0.2s ease;
  transition: transform 0.2s ease;
  box-shadow: 0 2rpx 8rpx rgba(99, 102, 241, 0.5);
}

.slider::-moz-range-thumb:hover {
  transform: scale(1.1);
  box-shadow: 0 4rpx 12rpx rgba(99, 102, 241, 0.7);
}

/* 输入框样�?*/

.input-group {
  margin-bottom: 20rpx;
}

.input-label {
  font-size: 15rpx;
  color: var(--text-secondary);
  margin-bottom: 8rpx;
  display: block;
  font-weight: 500;
}

.input-field {
  width: 100%;
  padding: 12rpx 16rpx;
  border: 2rpx solid var(--border);
  border-radius: 12rpx;
  font-size: 16rpx;
  transition: all 0.3s ease;
  background: rgba(26, 26, 46, 0.8);
  color: var(--text);
  backdrop-filter: blur(8rpx);
}

.input-field:focus {
  border-color: var(--primary);
  outline: none;
  box-shadow: 0 0 15rpx rgba(99, 102, 241, 0.3);
}

.input-field::-moz-placeholder {
  color: var(--text-secondary);
  opacity: 0.7;
}

.input-field::placeholder {
  color: var(--text-secondary);
  opacity: 0.7;
}

/* 复选框样式 */

.checkbox-group {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 20rpx;
}

.checkbox-label {
  font-size: 15rpx;
  color: var(--text-secondary);
  font-weight: 500;
}

/* 尺寸选项列表 */

.size-option {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 8rpx;
}

.size-option-label {
  font-size: 15rpx;
  color: var(--text-secondary);
  margin-bottom: 10rpx;
  display: block;
  font-weight: 500;
}

.size-option-btn {
  width: 100%;
  background: rgba(99, 102, 241, 0.1);
  color: var(--text);
  border: 2rpx solid var(--border);
  border-radius: 12rpx;
  padding: 14rpx 20rpx;
  font-family: 'Fredoka', sans-serif;
  font-size: 20rpx;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  backdrop-filter: blur(8rpx);
}

.size-option-btn:hover {
  background: rgba(99, 102, 241, 0.2);
  border-color: var(--primary);
  box-shadow: 0 0 10rpx rgba(99, 102, 241, 0.3);
}

.size-option-btn:active {
  background: rgba(99, 102, 241, 0.3);
  border-color: var(--primary-light);
}

/* 格式选择区域 */

.format-selector {
  margin-bottom: 15rpx;
}

.format-selector-title {
  font-size: 14rpx;
  color: var(--text-secondary);
  margin-bottom: 10rpx;
  display: block;
  font-weight: 500;
}

.format-options {
  display: flex;
  gap: 15rpx;
  align-items: center;
  flex-wrap: wrap;
}

.format-option {
  display: flex;
  align-items: center;
  gap: 5rpx;
}

.format-checkbox {
  width: 18rpx;
  height: 18rpx;
  border: 2rpx solid var(--border);
  border-radius: 4rpx;
  display: flex;
  justify-content: center;
  align-items: center;
  background: rgba(26, 26, 46, 0.8);
  transition: all 0.3s ease;
}

.format-checkbox:hover {
  border-color: var(--primary);
  box-shadow: 0 0 10rpx rgba(99, 102, 241, 0.3);
}

.format-checkbox.checked {
  background: var(--primary);
  border-color: var(--primary);
  box-shadow: 0 0 10rpx rgba(99, 102, 241, 0.5);
}

.format-checkbox-icon {
  font-size: 12rpx;
  color: var(--text);
}

.format-checkbox-label {
  font-size: 14rpx;
  color: var(--text-secondary);
}

/* 响应式设�?*/

@media (max-width: 768rpx) {
  .editor-content {
    padding: 16rpx;
  }
  
  .image-preview-section {
    padding: 20rpx;
    min-height: 250rpx;
  }
  
  .options-section {
    padding: 20rpx;
  }
  
  .bottom-buttons {
    padding: 20rpx 15rpx;
  }
  
  .bottom-btn {
    padding: 16rpx 20rpx;
    font-size: 14rpx;
  }
  
  .action-btn {
    padding: 14rpx 20rpx;
    font-size: 14rpx;
  }
  
  .format-grid {
    gap: 10rpx;
  }
  
  .format-btn {
    padding: 12rpx 10rpx;
    font-size: 13rpx;
  }
  
  .edit-buttons,
  .filter-buttons {
    flex-direction: column;
    gap: 10rpx;
  }
  
  .edit-buttons .action-btn,
  .filter-buttons .action-btn {
    margin-bottom: 0;
  }
}

/* 动画效果 */

@keyframes gradientMove {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

@keyframes pulse {
  0%, 100% {
    opacity: 1;
  }
  50% {
    opacity: 0.6;
  }
}

/* 对比视图样式 */

.comparison-container {
  height: 100%;
  position: relative;
  overflow: hidden;
}

.comparison-wrapper {
  height: 100%;
  position: relative;
  overflow: hidden;
}

.comparison-before {
  position: absolute;
  top: 0;
  left: 0;
  height: 100%;
  overflow: hidden;
  z-index: 2;
  border-right: 2rpx solid rgba(99, 102, 241, 0.5);
}

.comparison-after {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
}

.comparison-image {
  width: 100%;
  height: 100%;
  -o-object-fit: contain;
     object-fit: contain;
  position: absolute;
  top: 0;
  left: 0;
}

.comparison-label {
  position: absolute;
  top: 16rpx;
  padding: 8rpx 16rpx;
  border-radius: 12rpx;
  font-size: 14rpx;
  font-weight: 700;
  z-index: 3;
}

.comparison-label.before {
  left: 16rpx;
  background: rgba(99, 102, 241, 0.8);
  color: var(--text);
  backdrop-filter: blur(8rpx);
}

.comparison-label.after {
  right: 16rpx;
  background: rgba(34, 197, 94, 0.8);
  color: var(--text);
  backdrop-filter: blur(8rpx);
}

.comparison-slider {
  position: absolute;
  top: 0;
  height: 100%;
  width: 2rpx;
  background: var(--primary);
  transform: translateX(-50%);
  z-index: 4;
  cursor: ew-resize;
}

.slider-handle {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 40rpx;
  height: 40rpx;
  background: var(--primary);
  border-radius: 50%;
  display: flex;
  justify-content: center;
  align-items: center;
  box-shadow: 0 4rpx 12rpx rgba(99, 102, 241, 0.5);
  cursor: ew-resize;
  backdrop-filter: blur(8rpx);
}

.slider-icon {
  font-size: 12rpx;
  color: var(--text);
  font-weight: 700;
}

.comparison-hint {
  font-size: 14rpx;
  color: var(--text-secondary);
  text-align: center;
  margin-top: 16rpx;
  font-weight: 500;
  text-shadow: 0 0 5rpx rgba(99, 102, 241, 0.3);
}

/* 比较控制按钮样式 */

.comparison-controls {
  display: flex;
  flex-direction: row;
  justify-content: center;
  gap: 12rpx;
  margin-top: 20rpx;
  padding: 0 20rpx;
}

.control-btn {
  flex: 1;
  padding: 12rpx 16rpx;
  border-radius: 8rpx;
  background: rgba(26, 26, 46, 0.8);
  color: var(--text);
  border: 1rpx solid var(--border);
  font-size: 14rpx;
  font-weight: 600;
  text-align: center;
  cursor: pointer;
  transition: all 0.3s ease;
  backdrop-filter: blur(8rpx);
}

.control-btn:hover {
  background: rgba(99, 102, 241, 0.1);
  border-color: var(--primary);
  box-shadow: 0 0 10rpx rgba(99, 102, 241, 0.3);
}

.control-btn:active {
  background: rgba(99, 102, 241, 0.2);
  transform: translateY(1rpx);
}

/* 滚动条样�?*/

.scrollable-content::-webkit-scrollbar {
  width: 8rpx;
}

.scrollable-content::-webkit-scrollbar-track {
  background: rgba(26, 26, 46, 0.5);
  border-radius: 4rpx;
}

.scrollable-content::-webkit-scrollbar-thumb {
  background: var(--primary);
  border-radius: 4rpx;
}

.scrollable-content::-webkit-scrollbar-thumb:hover {
  background: var(--primary-light);
}

/*!******************************************************************************************************************************************************************************************************!*\
  !*** css ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[4].oneOf[0].use[1]!./node_modules/postcss-loader/dist/cjs.js??ruleSet[1].rules[4].oneOf[0].use[2]!./src/styles/CompressOptions.css ***!
  \******************************************************************************************************************************************************************************************************/
.compress-options {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  gap: 1rem;
}

.option {
  flex: 1 1 45%;
  min-width: 150rpx;
  margin-bottom: 0;
}

.option.custom {
  flex: 1 1 100%;
}

.option label {
  display: block;
  margin-bottom: 0.5rem;
  font-weight: 500;
  color: #44403C;
}

.option input[type="range"] {
  width: 100%;
  height: 8rpx;
  border-radius: 4rpx;
  background: #E7E5E4;
  outline: none;
  -webkit-appearance: none;
}

.option input[type="range"]::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: 20rpx;
  height: 20rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #7E22CE 0%, #9333EA 100%);
  cursor: pointer;
  box-shadow: 0 4rpx 12rpx rgba(126, 34, 206, 0.3);
  -webkit-transition: all 0.3s ease;
  transition: all 0.3s ease;
}

.option input[type="range"]::-webkit-slider-thumb:hover {
  transform: scale(1.1);
  box-shadow: 0 6rpx 16rpx rgba(126, 34, 206, 0.4);
}

.option input[type="range"]::-moz-range-thumb {
  width: 20rpx;
  height: 20rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #7E22CE 0%, #9333EA 100%);
  cursor: pointer;
  border: none;
  box-shadow: 0 4rpx 12rpx rgba(126, 34, 206, 0.3);
  -moz-transition: all 0.3s ease;
  transition: all 0.3s ease;
}

.option input[type="range"]::-moz-range-thumb:hover {
  transform: scale(1.1);
  box-shadow: 0 6rpx 16rpx rgba(126, 34, 206, 0.4);
}

