/*!***************************************************************************************************************************************************************************************************!*\
  !*** css ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[4].oneOf[0].use[1]!./node_modules/postcss-loader/dist/cjs.js??ruleSet[1].rules[4].oneOf[0].use[2]!./src/styles/CustomHeader.css ***!
  \***************************************************************************************************************************************************************************************************/
.custom-header {
  background-color: transparent;
}

.status-bar {
  width: 100%;
  background-color: transparent;
}

.custom-header-content {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 20rpx;
  background-color: transparent;
}

.custom-header-title {
  flex: 1;
  font-weight: 600;
  color: #333333;
  display: flex;
  align-items: center;
  justify-content: center;
}

.custom-header-right {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.header-icon {
  width: 30rpx;
  height: 30rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4rpx;
  background-color: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(10rpx);
}

.header-icon .icon {
  font-size: 16rpx;
  color: #333333;
}

.menu-icon {
  font-weight: bold;
}

/* 适配不同屏幕尺寸 */

@media screen and (max-width: 375px) {
  .custom-header-title {
    font-size: 16rpx;
  }
  
  .custom-header-content {
    padding: 0 16rpx;
  }
  
  .custom-header-right {
    gap: 16rpx;
  }
}

/*!************************************************************************************************************************************************************************************************!*\
  !*** css ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[4].oneOf[0].use[1]!./node_modules/postcss-loader/dist/cjs.js??ruleSet[1].rules[4].oneOf[0].use[2]!./src/pages/home/index.css ***!
  \************************************************************************************************************************************************************************************************/
.home {
  height: 100vh;
  background-color: var(--background);
  position: relative;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.home::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: radial-gradient(circle at 20% 50%, rgba(99, 102, 241, 0.1) 0%, transparent 50%),
              radial-gradient(circle at 80% 20%, rgba(236, 72, 153, 0.1) 0%, transparent 50%),
              radial-gradient(circle at 40% 80%, rgba(34, 197, 94, 0.1) 0%, transparent 50%);
  pointer-events: none;
}

.home-header {
  background: linear-gradient(135deg, var(--surface) 0%, var(--surface-light) 100%);
  padding: 40rpx 20rpx 30rpx;
  text-align: center;
  box-shadow: 0 4rpx 16rpx rgba(139, 92, 246, 0.1);
  border-bottom: 1rpx solid var(--border);
  position: relative;
  z-index: 1;
}

.home-title {
  font-family: 'Fredoka', sans-serif;
  font-size: 40rpx;
  font-weight: 700;
  color: var(--text);
  margin-bottom: 12rpx;
  display: block;
  text-shadow: var(--neon-glow);
  letter-spacing: -1rpx;
}

.home-subtitle {
  font-size: 20rpx;
  color: var(--text-secondary);
  margin-bottom: 32rpx;
  display: block;
  font-weight: 500;
  opacity: 0.9;
}

.home-content {
  flex: 1;
  overflow-y: auto;
  padding:20rpx 32rpx;
  position: relative;
  z-index: 1;
}

/* 主要功能区 */

.main-features-section {
  margin-bottom: 48rpx;
}

.main-features-grid {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 24rpx;
  position: relative;
  z-index: 1;
}

.main-feature-card {
  border-radius: 20rpx;
  padding: 20rpx;
  text-align: center;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
  cursor: pointer;
  position: relative;
  overflow: hidden;
  animation: cardReveal 0.6s ease forwards;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 24rpx;
}

.main-feature-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(99, 102, 241, 0.1), transparent);
  transition: left 0.5s ease;
}

.main-feature-card:hover::before {
  left: 100%;
}

.main-feature-card:hover {
  transform: translateY(-8rpx);
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.15);
}

.main-feature-card:active {
  transform: translateY(-2rpx);
}

.primary-feature {
  background: linear-gradient(135deg, #EDE7F6 0%, #F3E5F5 100%);
  border: 1rpx solid #D1C4E9;
}

.secondary-feature {
  background: linear-gradient(135deg, #F3E5F5 0%, #F8BBD0 100%);
  border: 1rpx solid #F48FB1;
}

.dpi-feature {
  background: linear-gradient(135deg, #E3F2FD 0%, #BBDEFB 100%);
  border: 1rpx solid #90CAF9;
}

.convert-feature {
  background: linear-gradient(135deg, #E0F7FA 0%, #B2EBF2 100%);
  border: 1rpx solid #80DEEA;
}

.main-feature-icon-wrap{
  height: 80rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.main-feature-icon {
  font-size: 80rpx;
  display: block;
}

.primary-feature .main-feature-icon {
  font-size: 80rpx;
  position: relative;
  z-index: 1;
}

.main-feature-title {
  font-family: 'Fredoka', sans-serif;
  font-size: 30rpx;
  font-weight: 600;
  color: #4A148C;
  display: block;
  position: relative;
  z-index: 1;
}

.dpi-feature .main-feature-title,
.convert-feature .main-feature-title {
  color: #1A237E;
}

.main-feature-btn {
  display: inline-block;
  padding: 12rpx 24rpx;
  border-radius: 16rpx;
  background: rgba(156, 39, 176, 0.2);
  color: #7B1FA2;
  font-size: 20rpx;
  font-weight: 600;
  transition: all 0.3s ease;
  position: relative;
  z-index: 1;
}

.main-feature-card:hover .main-feature-btn {
  background: #7B1FA2;
  color: white;
  box-shadow: 0 0 12rpx rgba(123, 31, 162, 0.6);
}

.dpi-feature .main-feature-btn {
  background: rgba(25, 118, 210, 0.2);
  color: #1565C0;
}

.dpi-feature:hover .main-feature-btn {
  background: #1565C0;
  color: white;
  box-shadow: 0 0 12rpx rgba(21, 101, 192, 0.6);
}

.convert-feature .main-feature-btn {
  background: rgba(0, 150, 136, 0.2);
  color: #00796B;
}

.convert-feature:hover .main-feature-btn {
  background: #00796B;
  color: white;
  box-shadow: 0 0 12rpx rgba(0, 121, 107, 0.6);
}

/* 更多功能区 */

.more-features-section {
  margin-bottom: 40rpx;
}

.more-features-title {
  font-family: 'Fredoka', sans-serif;
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text);
  margin-bottom: 24rpx;
  display: block;
  text-shadow: 0 0 5rpx rgba(255, 255, 255, 0.1);
}

.more-features-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20rpx;
  position: relative;
  z-index: 1;
}

.more-feature-card {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 16rpx;
  padding: 40rpx 0;
  text-align: center;
  transition: all 0.3s ease;
  cursor: pointer;
  position: relative;
  overflow: hidden;
  animation: cardReveal 0.6s ease forwards;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 24rpx;
}

.more-feature-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(139, 92, 246, 0.05), transparent);
  transition: left 0.5s ease;
}

.more-feature-card:hover::before {
  left: 100%;
}

.more-feature-card:hover {
  transform: translateY(-4rpx);
  box-shadow: var(--neon-glow), 0 6rpx 24rpx rgba(0, 0, 0, 0.1);
  border-color: var(--primary);
}

.more-feature-card:active {
  transform: translateY(-2rpx);
}

.more-feature-icon-wrap{
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.more-feature-icon {
  font-size: 60rpx;
  display: block;
}

.more-feature-title {
  font-family: 'Fredoka', sans-serif;
  font-size: 24rpx;
  font-weight: 600;
  color: var(--text);
  display: block;
  line-height: 1.3;
}

.feature-status {
  position: absolute;
  top: 0;
  right: 0;
  background: rgba(0,0,0,0.3);
  border-radius: 0 16rpx 0 16rpx;
  padding: 4rpx 8rpx;
  font-size: 16rpx;
  color: #fff;
}

.footer {
  text-align: center;
  padding: 24rpx;
  color: var(--text-secondary);
  font-size: 14rpx;
  font-weight: 500;
  background: var(--surface);
  border-top: 1rpx solid var(--border);
  position: relative;
  z-index: 1;
  margin-top: 40rpx;
}

/* 响应式设�?*/

@media (max-width: 768rpx) {
  .home-header {
    padding: 40rpx 20rpx 30rpx;
  }
  
  .home-title {
    font-size: 48rpx;
  }
  
  .home-subtitle {
    font-size: 24rpx;
    margin-bottom: 32rpx;
  }
  
  .home-content {
    padding: 24rpx;
  }
  
  .main-features-grid {
    grid-template-columns: 1fr;
    grid-template-rows: auto auto auto;
  }
  
  .primary-feature {
    grid-row: 1 / 2;
    grid-column: 1 / 2;
  }
  
  .secondary-feature {
    grid-row: auto;
    grid-column: 1 / 2;
  }
  
  .more-features-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 16rpx;
  }
  
  .main-feature-card {
    padding: 28rpx 16rpx;
  }
  
  .main-feature-icon {
    font-size: 56rpx;
    margin-bottom: 20rpx;
  }
  
  .primary-feature .main-feature-icon {
    font-size: 64rpx;
  }
  
  .main-feature-title {
    font-size: 22rpx;
  }
  
  .primary-feature .main-feature-title {
    font-size: 24rpx;
  }
  
  .more-feature-card {
    padding: 20rpx 12rpx;
  }
  
  .more-feature-icon {
    font-size: 36rpx;
    margin-bottom: 10rpx;
  }
  
  .more-feature-title {
    font-size: 14rpx;
  }
}

/* 动画效果 */

@keyframes cardReveal {
  from {
    opacity: 0;
    transform: translateY(30rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.main-feature-card,
.more-feature-card {
  animation: cardReveal 0.6s ease forwards;
}

.main-feature-card:nth-child(1) { animation-delay: 0.1s; }

.main-feature-card:nth-child(2) { animation-delay: 0.2s; }

.main-feature-card:nth-child(3) { animation-delay: 0.3s; }

.more-feature-card {
  animation-delay: 0.4s;
}

/* 扫描线效�?*/

.scanline-effect {
  position: relative;
}

.scanline-effect::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: var(--scanline);
  background-size: 100% 4rpx;
  pointer-events: none;
  animation: scan 10s linear infinite;
  opacity: 0.1;
}

/* 加载动画 */

.loading-shimmer {
  background: linear-gradient(90deg, transparent, rgba(99, 102, 241, 0.2), transparent);
  background-size: 200% 100%;
  animation: shimmer 2s infinite;
}

@keyframes shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}

/* 霓虹按钮效果 */

.neon-button {
  background: transparent;
  border: 2rpx solid var(--primary);
  color: var(--primary);
  padding: 12rpx 24rpx;
  border-radius: 12rpx;
  font-family: 'Fredoka', sans-serif;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.neon-button:hover {
  background: var(--primary);
  color: white;
  box-shadow: var(--neon-glow);
  transform: translateY(-2rpx);
}

.neon-button:active {
  transform: translateY(0);
}

.neon-button::before {
  content: '';
  position: absolute;
  top: -2rpx;
  left: -2rpx;
  right: -2rpx;
  bottom: -2rpx;
  background: linear-gradient(45deg, var(--primary), var(--secondary), var(--cta), var(--primary));
  border-radius: 12rpx;
  z-index: -1;
  opacity: 0;
  transition: opacity 0.3s ease;
  animation: gradientMove 3s linear infinite;
}

.neon-button:hover::before {
  opacity: 1;
}

@keyframes gradientMove {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

